name: Nightly
on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  nightly:
    uses: parthalon025/ci-workflows/.github/workflows/reusable-nightly.yml@v1
    with:
      language: 'node'
    secrets: inherit
