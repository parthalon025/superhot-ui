name: Release
on:
  push:
    branches: [main]

jobs:
  release:
    uses: parthalon025/ci-workflows/.github/workflows/reusable-release.yml@v1
    with:
      release-type: 'node'
    secrets: inherit
    permissions:
      contents: write
      pull-requests: write
